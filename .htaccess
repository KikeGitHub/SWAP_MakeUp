# Performance cache policy for static assets
# Requires Apache modules: mod_headers, mod_expires

<IfModule mod_expires.c>
  ExpiresActive On

  # Long cache for fingerprinted/minified static assets
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"

  # Keep HTML fresh
  ExpiresByType text/html "access plus 0 seconds"
</IfModule>

<IfModule mod_headers.c>
  # Immutable long cache for minified CSS/JS
  <FilesMatch "\.(min\.css|min\.js)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

  # Avoid stale HTML
  <FilesMatch "\.(html)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires "0"
  </FilesMatch>
</IfModule>
